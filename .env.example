DATABASE_URL=postgresql+psycopg://artist_growth:artist_growth@localhost:5432/artist_growth
MEDIA_ROOT=./var/media
API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
RENDER_ROOT=./var/renders
JOB_LEASE_SECONDS=600
JOB_POLL_SECONDS=1.0
# Generate separate random values (32+ characters). Never reuse one credential for both capabilities.
CONTROL_PLANE_READ_TOKEN=
JOB_API_TOKEN=
# Private-alpha web gate. Deploy behind HTTPS. Password must be at least 24 characters.
WEB_OPERATOR_USERNAME=
WEB_OPERATOR_PASSWORD=
